–––––––––––– script.rb ––––––––––––

def build(data:)
  render(name: 'foo', content: data["image-data"].png)  # By passing through pathname
  render(name: 'bar', content: data["image-data"].png.binread)  # By reading content
end
